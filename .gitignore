node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
next-env.d.ts
.venv
venv

# Datos de operación: catálogo, pedidos y leads reales.
# Se versiona solo el catálogo; pedidos y leads son datos de clientes.
/data/orders.json
/data/leads.json

# Fotos originales y material de referencia (pesan y no van al deploy)
/_originales
tsconfig.tsbuildinfo
